package data

// Parameters holds the input for the API API call
type Parameters struct {
	APIKey, Timeframe, Tilt, Losses, Azimuth, Latitude, Longitude, Capacity, ArrayType, ModuleType, Zoom string
}
